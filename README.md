# agent-101

this is a fun project to understand how agents work by building an observability tool to monitor what the agents are doing in prod.

currently building it on the vercel-ai-sdk.

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```
